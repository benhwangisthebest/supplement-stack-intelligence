# Phase 4 — decision queue

> **Append-only** (runner specification item 5, plan §6 D-1). A RUNNER unit writes a question here instead of
> deciding it. An entry never blocks independent work. A unit waiting on one parks, and a batch-bearing landing
> waits for its answer. The owner answers inline, dated. The runner never writes an answer.

| # | Date | Unit | Question | Options | Blocks |
|---|---|---|---|---|---|
| Q-1 | 2026-09-25 | U1 | **C-2's shrink-only artifact pin covers `docs/01-plan/features/context-adjusted-evidence.plan.md` (399 lines at `66e6804`), the file U14a exists to revise.** Under ARTIFACT_CAP (`criteria-parity.test.ts:283`) U14a may not grow it. | (a) U14a revises it within 399 lines · (b) U14a writes the revision as a new artifact ≤ 200 lines and the old file stays as history · (c) a dated exception raising that one pin, recorded with its reason | nothing now; U14a |
| Q-2 | 2026-09-25 | U1 | **The brief named "one shared anchored stripper". U1 shipped a string-aware one instead** (`src/architecture/__testing__/strip.ts`): it strips a `//` comment only where it starts outside a literal. It was measured first: the anchored `/^\s*\/\/.*$/gm` would have stopped stripping 285 trailing comments, which weakens every presence check that used it. On the tracked tree the helper only reveals literals containing `//` (96 lines over tracked `src/**/*.ts(x)` and `scripts/**/*.{ts,mjs}` outside `src/architecture/`). A second reviewer confirmed against the TypeScript parser that every stripped line comment matches exactly. The 285 is over `src/**/*.ts(x)` and `scripts/**/*.ts` outside `src/architecture/`, at `66e6804`. On adversarial input it is a heuristic: the reviewers built seven constructs where an earlier version kept a real comment, and the helper was hardened so that none does, with those seven pinned as self-tests. **One known limit remains:** a misread BACKTICK (a regex containing one right after `)`, or a lone backtick in JSX text) keeps the comments up to the next backtick. That is weaker than the pre-U1 regex on that input, and it is stated in the helper's header. No such construct is in the tree. It is **not** a lexer. **Confirm the deviation, or direct a change.** | (a) keep the string-aware helper · (b) switch to the anchored form and accept the trailing-comment exposure · (c) another shape | nothing; U1 landed under (a) pending the answer |
| Q-3 | 2026-09-25 | U1 | **`CLAUDE.md` §5's measured baseline now differs from the tree.** U1 adds a linted helper and 22 tests: lint 415 → **416** of 416, and unit tests 1693 → **1715** (node 1563 → 1585). §5 calls those figures a drifting snapshot, and no guard binds them, so nothing is red. The runner may not edit `CLAUDE.md`. | (a) re-measure at the Phase 4 closeout · (b) an owner-approved §5 update now | nothing |
